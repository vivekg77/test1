/*
 * Copyright 2021 Metro Bank. All rights reserved.
 */
package com.metrobank.payments.sepa.inbound.bl.connector;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class PaymentsSepaBlConnectorApplicationTests {

    /**
     * A test method to test if the context loads
     */
    @Test
    void contextLoads() {
        given:{
            // the context is created successfully
        }
        when:{
            // the context is loaded successfully
        }
        then:{
            Assertions.assertTrue(true);
        }
    }

}
